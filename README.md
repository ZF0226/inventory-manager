# 进入项目目录
cd /path/to/inventory-manager

# 初始化 Git
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit"

# 添加远程仓库（将 YOUR_USERNAME 替换为您的 GitHub 用户名）
git remote add origin https://github.com/ZF0226/inventory-manager.git

# 推送到 GitHub
git branch -M main
git push -u origin main
